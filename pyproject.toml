[project]
name = "ComfyUI-RE-CustomUtils"
version = "0.6.1"
description = "A collection of custom nodes for ComfyUI."
license = { file = "LICENSE" }
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "torch>=2.12.0",
]

[project.urls]
Repository = "https://github.com/relhamdi/ComfyUI-RE-CustomUtils"

[dependency-groups]
dev = [
    "pytest>=9.0.3",
    "pytest-aiohttp>=1.1.0",
]
